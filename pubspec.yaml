name: anyfeast_onboarding_redesign
description: Minimal Flutter onboarding redesign for AnyFeast internship.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
